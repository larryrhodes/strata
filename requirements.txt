# Currently fetch_kev.py only uses the Python standard library (json, urllib, pathlib).
# This file is here for when we add more sources that need real HTTP clients / parsers,
# e.g.:
# requests>=2.31
# feedparser>=6.0
